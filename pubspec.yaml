name: digidocs
description: A digital archiving system built with Flutter for Windows.
publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite_common_ffi: ^2.3.0
  path: ^1.9.0
  file_picker: ^8.0.0

flutter:
  uses-material-design: true
